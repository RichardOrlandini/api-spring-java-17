spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/banco-api?createDatabaseIfNotExist=true
    username: postgres
    password: postgres123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show-sql: true
        format_sql: true
